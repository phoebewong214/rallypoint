__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/
.env
*.db
*.sqlite3
instance/
# Alembic migration scripts MUST be committed (the deploy runs `flask db
# upgrade` against them) — only ignore their compiled cache.
migrations/versions/__pycache__/
