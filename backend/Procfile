web: python manage.py upgrade-db && (python manage.py import-courts --if-empty || echo "import-courts skipped/failed, continuing") && gunicorn wsgi_gevent:app --bind 0.0.0.0:$PORT -k gevent --workers 1 --timeout 60
